spring:
  application:
    name: provider-service
  datasource:
    url: jdbc:mysql://192.168.56.10/provider_db?useSSL=false&serverTimeZone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.kawasaki.service.common.domain

server:
  port: 9000

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka